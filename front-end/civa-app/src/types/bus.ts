export interface Bus {
  id: number;
  numero: string;
  placa: string;
  fechaCreacion: string;
  caracteristicas: string;
  marca: string;
  activo: boolean;
}

// tus propiedades aquí, por ejemplo:
